# 游戏电竞 IP 推荐工具 — 使用说明

## 一、这是什么

一个纯前端网页工具：同事填写合作需求（预算、目标人群、游戏品类、合作场景等），
点"开始推荐"后，AI（DeepSeek）自动分析并推荐合适的游戏电竞 IP。

**同事零配置即用**：打开网页就能直接出结果，不需要填任何密钥。AI 调用走的是
内置的统一密钥（你的 DeepSeek 账号额度）。

## 二、文件说明

| 文件 | 作用 |
|---|---|
| `index.html` | 网页主体 |
| `styles.css` | 样式 |
| `app.js` | 交互逻辑、调用 AI |
| `data.js` | IP 数据库 + **内置的 AI 密钥配置（`BUILTIN_LLM`）** |

## 三、本地使用

直接用浏览器打开 `index.html` 即可。

## 四、给同事用（部署）

参考你同事的做法，最简单的是 **GitHub Pages**：

1. 新建一个 GitHub 仓库，把这 4 个文件传上去。
2. 仓库 Settings → Pages → 选择分支 → 保存，几分钟后会得到一个
   `https://你的用户名.github.io/仓库名/index.html` 的网址。
3. 把网址发给同事即可，无需任何配置。

> 内网同事访问 GitHub Pages 如有网络限制，可改用公司内部静态托管，
> 把这 4 个文件丢上去同样能用。

## 五、⚠️ 重要安全提醒（务必看）

`data.js` 里**写死了你的 DeepSeek API Key**。这意味着：

- **任何打开网页的人，按 F12 看源码就能拿到这个 Key**，进而盗刷你的额度。
- 所以这个网址**只能发给可信的公司内部同事**，不要对外公开、不要发到群外。
- 如果发现额度异常消耗，立刻去 https://platform.deepseek.com/ **重置/删除该 Key**，
  生成新 Key 后替换 `data.js` 里 `BUILTIN_LLM.apiKey` 的值即可。
- **不要把含 Key 的代码上传到任何"公开"仓库**。上传 GitHub 时务必把仓库设为
  **Private**，或部署到公司内网。

## 六、维护：更换 Key / 模型 / 账号

打开 `data.js`，找到文件顶部的 `BUILTIN_LLM`：

```javascript
const BUILTIN_LLM = {
  provider: 'deepseek',
  baseUrl: 'https://api.deepseek.com/chat/completions',
  apiKey: 'sk-xxxxxxxx',        // ← 换 Key 改这里
  model: 'deepseek-chat'        // ← 换模型改这里（deepseek-chat / deepseek-reasoner）
};
```

- **换 Key**：改 `apiKey`。
- **换模型**：`deepseek-chat`（快、便宜，推荐）或 `deepseek-reasoner`（更强的推理，更贵更慢）。
- 改完保存，刷新网页即可生效。

## 七、费用

DeepSeek 是按量付费（用多少扣多少），deepseek-chat 价格很低。
内部小范围使用，充值 10~50 元通常能用很久。余额不足时网页会提示
`Insufficient Balance`，去开放平台充值即可，代码无需改动。
